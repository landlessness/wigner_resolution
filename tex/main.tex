\documentclass[aps,pra,reprint,amsmath,amssymb,floatfix,nofootinbib]{revtex4-2}

\usepackage{graphicx}
\usepackage{bm}

\begin{document}

\title{Wigner Resolution from Action Capacity}

\author{Brian S. Mulloy}
\email{bmulloy@umich.edu}

\date{\today}

\begin{abstract}
This is the abstract.
\end{abstract}

\maketitle

% ===============================================================================
%  THE PHASE-SPACE CELLS
% ===============================================================================

\begin{figure}[!htbp]
\centering
\includegraphics[width=\linewidth]{figures/de_gosson_cells.pdf}
\caption{\textbf{de Gosson cells.} The state's covariance defines the extended cell of action capacity $A = \pi\Delta x\Delta p \ge h/2$. Inscribed within it is a one-parameter family of squeezed cells of fixed action capacity $h/2$, each parameterized by orientation $\theta$. The semi-axes of any squeezed cell satisfy $\delta_\parallel\delta_\perp = \hbar$.}
\label{fig:de_gosson_cells}
\end{figure}

\begin{figure}[!htbp]
\centering
\includegraphics[width=\linewidth]{figures/zurek_cells.pdf}
\caption{\textbf{Zurek cells.} At $\theta = 0$, $\delta_\parallel = \Delta x$ and $\delta_\perp = \delta p$. At $\theta = \pi/2$, $\delta_\parallel = \Delta p$ and $\delta_\perp = \delta x$. The minor axes $\delta x = \hbar / \Delta p$ and $\delta p = \hbar / \Delta x$ are the interference scales identified by Zurek.}
\label{fig:zurek_cells}
\end{figure}

We work in one degree of freedom and align coordinates with the principal axes of the state's covariance ellipse. The Robertson--Schr\"odinger covariance term~\cite{robertson1929,schrodinger1930} vanishes, leaving the position spread $\Delta x = \sqrt{2}\sigma_x$ and the momentum spread $\Delta p = \sqrt{2}\sigma_p$. The symplectic results we use~\cite{degosson2013qblobs,degossoncharlyne2022} extend without modification to higher dimensions and to the full off-diagonal covariance.

The state's covariance defines two phase-space cells (Fig.~\ref{fig:de_gosson_cells}). The extended cell, of action capacity
\begin{equation}
A = \pi\,\Delta x\,\Delta p \ge h/2,
\label{eq:extended_cell}
\end{equation}
is the state's outer extent. Inscribed within it is a one-parameter family of squeezed cells of fixed action capacity
\begin{equation}
a = \pi\,\delta_\parallel\,\delta_\perp = h/2
\label{eq:squeezed_cell}
\end{equation}
identified by de Gosson~\cite{degosson2013qblobs,degossoncharlyne2022}. The semi-axes obey the polar-dual identity
\begin{equation}
\delta_\parallel\,\delta_\perp = \hbar.
\label{eq:polar_dual}
\end{equation}
The area of the cell is fixed at $h/2$ by Gromov's non-squeezing theorem~\cite{gromov1985}. As the orientation $\theta$ varies,
\begin{equation}
\delta_\parallel(\theta) = \sqrt{\Delta x^2\cos^2\theta + \Delta p^2\sin^2\theta},
\label{eq:delta_parallel}
\end{equation}
\begin{equation}
\delta_\perp(\theta) = \hbar / \delta_\parallel(\theta),
\label{eq:delta_perp}
\end{equation}
where $\theta$ is the rotated quadrature direction of continuous-variable tomography~\cite{vogelrisken1989,lvovsky2009}. The minor axis is considered squeezed and the major axis anti-squeezed~\cite{teichsaleh1989}. At the principal-axis orientations $\theta = 0$ and $\theta = \pi/2$ (Fig.~\ref{fig:zurek_cells}), the minor axes reduce to the interference scales identified by Zurek~\cite{zurek2001},
\begin{equation}
\delta x = \frac{\hbar}{\Delta p}, \qquad \delta p = \frac{\hbar}{\Delta x}.
\label{eq:zurek}
\end{equation}

% ===============================================================================
%  THE BOUNDARY AND THE KERNEL
% ===============================================================================

The Wigner function of the state is
\begin{equation}
W(x,p) = \frac{1}{\pi\hbar}\int \psi^*(x+s)\,\psi(x-s)\,e^{2ips/\hbar}\,ds.
\label{eq:wigner}
\end{equation}

The kernel
\begin{equation}
K_\theta(x,p) = \frac{1}{\pi\hbar}\exp\!\left(-\frac{x_\theta^2}{\delta_\parallel^2} - \frac{p_\theta^2}{\delta_\perp^2}\right),
\label{eq:Gtheta}
\end{equation}
with $x_\theta = x\cos\theta + p\sin\theta$ and $p_\theta = -x\sin\theta + p\cos\theta$ has action capacity $h/2$. The state seen at this resolution is
\begin{equation}
P_\theta(x,p) = (W * K_\theta)(x,p).
\label{eq:Ptheta}
\end{equation}
This is non-negative everywhere. The convolution of two Wigner functions equals the trace inner product of the corresponding density operators~\cite{degosson2005bullsci,degosson2013qblobs}, which is non-negative for any pair of states. The non-negativity is a standard fact of the de Gosson and quantum-optics literature. What is new is the kernel's uniqueness. Its covariance is fixed by the state, its action is fixed at $h/2$, and its orientation $\theta$ ranges over the inscribed family.

% ===============================================================================
%  DEMONSTRATION
% ===============================================================================

\begin{figure}[!htbp]
\centering
\includegraphics[width=\linewidth]{figures/eigen.pdf}
\caption{\textbf{Eigenstates.} Squeezed vacuum at $r=0.5$, harmonic Fock state ($n=1$), Morse ($n=8$), and double-well ($n=5$). \textit{Left}, $W(x,p)$ with the extended cell and the $\theta=\pi/2$ squeezed cell overlaid. \textit{Center}, $W(x,0)$. \textit{Right}, $P_{\pi/2}(x,0)$.}
\label{fig:eigen}
\end{figure}

\begin{figure}[!htbp]
\centering
\includegraphics[width=\linewidth]{figures/cat.pdf}
\caption{\textbf{Cat states.} 2-cat, 3-cat, 4-cat square, and 4-cat compass states. Columns as in Fig.~\ref{fig:eigen}.}
\label{fig:cat}
\end{figure}

Figures~\ref{fig:eigen} and~\ref{fig:cat} show $P_{\pi/2}$ for eight states: squeezed vacuum, harmonic, Morse, and double-well eigenstates, and four cat configurations including Zurek's compass. With the kernel set from each state's covariance, the same number of positive peaks of $W$ along the chosen cross-section appear in $P_{\pi/2}$, without negativity.

\bibliography{main}

\end{document}
